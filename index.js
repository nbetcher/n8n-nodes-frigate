// This package exposes its nodes and credentials through the `n8n` key in
// package.json. n8n loads the compiled files in dist/ directly, so this entry
// point intentionally exports nothing.
module.exports = {};
